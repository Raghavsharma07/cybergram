import React from 'react';
import { LoginHeader } from '../../components/header';
import { Feed } from '../../components/feed';
const Home = () => {
    return (
        <div>
            <LoginHeader />

            <Feed />
        </div>
    );
};

export default Home;
